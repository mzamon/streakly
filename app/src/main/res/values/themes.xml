<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Streakly" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Streakly Color Palette -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/primary_light</item>
        <item name="colorOnPrimaryContainer">@color/background_primary</item>

        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="colorSurface">@color/surface_card</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorSurfaceVariant">@color/background_secondary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>

        <item name="android:windowBackground">@color/background_primary</item>
        <item name="android:statusBarColor">@color/background_primary</item>
        <item name="android:navigationBarColor">@color/background_secondary</item>

        <!-- Shape & Typography accents -->
        <item name="textAppearanceTitleLarge">@style/TextAppearance.Streakly.Title</item>
    </style>

    <style name="Theme.Streakly" parent="Base.Theme.Streakly" />

    <style name="Theme.Streakly.Splash" parent="Theme.Streakly">
        <item name="android:windowBackground">@color/background_primary</item>
    </style>

    <style name="TextAppearance.Streakly.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>